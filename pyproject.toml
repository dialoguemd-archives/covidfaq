[tool.poetry]
name = "covidfaq"
version = "0.1.0"
description = ""
authors = ["dialogue"]

[tool.poetry.dependencies]
python = "^3.7"
rasa = {version = "1.9.3", extras = ["spacy"]}

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry>=1.0.0"]
build-backend = "poetry.masonry.api"
